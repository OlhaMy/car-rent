export const selectCars = (state) => state.car.cars;
export const selectFavorites = (state) => state.car.favorites;
export const selectMakes = (state) => state.car.makes;
export const selectPrices = (state) => state.car.prices;
export const selectStatus = (state) => state.car.status;
export const selectError = (state) => state.car.error;
